#! /usr/bin/env bash

# for file in *.wav; do mv "$file" $(echo $file | tr ' ' '_') ; done
# exit 0

for file in *.wav; do
  ffmpeg -i "${file}" -codec:a libmp3lame -qscale:a 2 "${file[@]/%wav/mp3}"
done
